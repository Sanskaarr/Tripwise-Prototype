package com.tripwise.config;

import org.springframework.context.annotation.Configuration;

// CORS is configured in SecurityConfig.corsConfigurationSource() to avoid duplicate headers.
@Configuration
public class WebConfig {
}
